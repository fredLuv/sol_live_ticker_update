services:
  - type: web
    name: sol-live-ticker-update
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash scripts/start_render.sh
    healthCheckPath: /live_crypto_dashboard.html
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
